# Machine-learning-guided prospective discovery of a process-relevant Pd replacement candidate for furfural hydrogenation

> **Repository status:** private, pre-publication reproducibility package. The repository will remain private until the authors approve the final public release.

This repository contains the Python code, fixed machine-learning datasets, optimized model artifacts, bootstrap results, and plotting scripts supporting the manuscript *Machine-learning-guided prospective discovery of a process-relevant Pd replacement candidate for furfural hydrogenation*.

The repository does **not** contain the manuscript PDF, Supplementary Information document, submission files, or unpublished internal review material.

## Overview

The computational workflow covers:

- evaluation and optimization of nine regression models;
- interpretation of the optimized XGBoost model using SHAP and partial-dependence analyses;
- Pd-relative operating-window mapping and non-Pd catalyst prioritization;
- training-row bootstrap stability analysis;
- comparison of model predictions with prospective experiments; and
- process-level yield, cost, minimum-selling-price, and sensitivity analyses.

## Repository structure

```text
.
|-- base_model/                  Baseline model training scripts
|-- bootstrap_output/           Precomputed 200-refit bootstrap results
|-- dataset/                    Fixed datasets from the supplied archive
|-- figures/
|   |-- common.py               Shared data/model loading and output helpers
|   |-- main/                   Main-text figure scripts
|   `-- supplementary/          Supplementary figure scripts
|-- hyperparameter_tuning/      Grid-search scripts and optimized models
|-- source_data/                Figure-specific derived source-data files
|-- tests/                      Repository and data-integrity checks
|-- environment.yml             Exact Conda environment
|-- requirements.txt            Exact pip dependencies
|-- CITATION.cff                Citation metadata
`-- README.md
```

The files and names inside `dataset/` are intentionally retained from the supplied archive.

## Installation

Python 3.10.18 was used for the manuscript analyses.

### Conda (recommended)

```bash
conda env create -f environment.yml
conda activate thfa-catalyst-ml
```

### pip

```bash
python -m venv .venv
```

On Windows:

```bash
.venv\Scripts\activate
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

On Linux or macOS:

```bash
source .venv/bin/activate
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

This repository uses Git LFS for trained model artifacts. Clone with Git LFS enabled:

```bash
git lfs install
git clone <repository-url>
cd Machine-learning-guided-process-relevant-replacement-of-Pd-catalysts-for-furfural-hydrogenation
git lfs pull
```

## Figure reproduction

Run commands from any directory; each script resolves the repository root automatically. PNG files are written to `output/figures/png/` and editable-text PDFs to `output/figures/pdf/`.

### Main-text figures

| Manuscript panel | Command | Scope |
|---|---|---|
| Fig. 2 | `python figures/main/figure_02.py` | Predictive performance, SHAP and PDP analyses |
| Fig. 3 | `python figures/main/figure_03.py` | Pd-relative operating window and bootstrap rank stability |
| Fig. 4a | `python figures/main/figure_04a.py` | Experimental and model-predicted THFA yields |
| Fig. 6b-c | `python figures/main/figure_06bc.py` | Yield-cost and yield-MSP comparisons |
| Fig. 6d | `python figures/main/figure_06d.py` | Catalyst-lifetime sensitivity |

Fig. 6d was labelled Supplementary Fig. 19 in an earlier code draft. It is mapped to main-text Fig. 6d in this repository to match the final manuscript numbering.

### Supplementary figures

| Supplementary figure | Command |
|---|---|
| Fig. S1 | `python figures/supplementary/figure_s01.py` |
| Fig. S2 | `python figures/supplementary/figure_s02.py` |
| Fig. S3 | `python figures/supplementary/figure_s03.py` |
| Fig. S4 | `python figures/supplementary/figure_s04.py` |
| Fig. S5 | `python figures/supplementary/figure_s05.py` |
| Fig. S6 | `python figures/supplementary/figure_s06.py` |
| Fig. S7 | `python figures/supplementary/figure_s07.py` |
| Fig. S8 | `python figures/supplementary/figure_s08.py` |
| Fig. S17 | `python figures/supplementary/figure_s17.py` |
| Fig. S18 | `python figures/supplementary/figure_s18.py` |

Main-text Fig. 1, Fig. 4b-h, Fig. 5, Fig. 6a, and Supplementary Figs. 9-16 are schematic or experimental-characterization panels and are not generated by the Python code in this repository.

## Dataset

The `dataset/` directory contains:

- the preprocessed literature-derived dataset (2,132 records);
- the fixed training input and target files (1,956 records);
- the fixed test input and target files (177 records); and
- a pickle file containing molar masses used to construct catalyst cases.

The fixed split contains 161 model input features. A random seed of 23 was used where applicable for splitting, model training, optimization, and nominal evaluation.

## Optimized models

The optimized model artifacts are stored in `hyperparameter_tuning/output/` and managed with Git LFS. The main analyses use `xgb_model_seed_23.json`. The other optimized models reproduce the nine-model comparison in Supplementary Fig. 2.

Do not use GitHub source-code ZIP downloads for this repository because those archives can contain Git LFS pointer files instead of the model objects. Clone the repository and run `git lfs pull`.

## Bootstrap analysis

`bootstrap_output/bootstrap_ensemble_results.npz` contains results from 200 training-row bootstrap refits. The manuscript Figure 3 and Supplementary Fig. 8 use these precomputed results by default. The full refitting implementation and seeds are retained in `figures/supplementary/figure_s08.py`.

## Validation

Run the repository checks with:

```bash
python -m unittest discover -s tests -v
python -m compileall figures base_model hyperparameter_tuning
```

Small numerical differences may occur across operating systems, processor architectures, parallel-computing settings, and package builds. The exact package versions are recorded in `environment.yml` and `requirements.txt`.

## Citation

Citation metadata are provided in `CITATION.cff`.

## Release plan

The private preparation history uses version `0.1.0`. After the public code license is approved, the first public snapshot can be tagged `v1.0.0` and accompanied by a GitHub release. The detailed checks are recorded in `RELEASE_CHECKLIST.md`.

## License

All rights are reserved during private pre-publication preparation. An open-source code license and any separate data-licensing terms will be finalized by the authors before the repository is made public.
