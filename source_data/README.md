# Figure source data

This directory contains derived source-data files used for figure verification.
It is separate from the fixed machine-learning files in `dataset/`, whose
structure and filenames are preserved from the supplied archive.

`figure_03_bootstrap_source_data.xlsx` contains the source-data export for the
200-refit XGBoost bootstrap analysis supporting manuscript Figure 3.

